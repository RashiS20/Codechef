#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin>>t;
    while(t--)
    {
        int n;
        cin>>n;
        int c_one=0;
        vector<int> arr(n);
        for(int i=0;i<n;i++)
        cin>>arr[i];
         for(int i=0;i<n;i++)
        {
            if(arr[i]==1)
            c_one++;
            /*else
            c_zero++;*/
        }
        if(c_one%2==0)
        cout<<"YES"<<endl;
        else
        cout<<"NO"<<endl;
    }
	// your code goes here
	return 0;
}
